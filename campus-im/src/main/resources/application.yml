server:
  port: 8090

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: campus_im
    redis:
      host: localhost
      port: 6379
      password: ***REMOVED***
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

im:
  websocket:
    port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
