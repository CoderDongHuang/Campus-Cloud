server:
  port: 8089

spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: ${REDIS_PASSWORD}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: ${MQ_PASSWORD}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
