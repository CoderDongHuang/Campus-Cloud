spring:
  application:
    name: campus-settlement
  cloud:
    nacos:
      discovery:
        enabled: true
      config:
        enabled: true

jasypt:
  encryptor:
    password: ${JASYPT_PASSWORD}
